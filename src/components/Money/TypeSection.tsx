import styled from 'styled-components';
import React from 'react';

const TypeSection = styled.section`
  
`

const Type=()=>{
  return(
    <TypeSection>
    </TypeSection>
  )
}

export default Type